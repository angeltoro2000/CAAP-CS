Angel Toro/ angeltoro

Hello.py: It simply prints "Hello!"

Converter.py: It converts from Fahrenheit to Celsius, it accepts all numbers, but inputs floats.

Unit conversion.py: Works for all ints and floats. It changes from centimeters to inches. 

Slope.py: This will give you the addition of a specified number of integers. It only works for int values to find out how many. I.e it can't add 2.3 numbers.  

fibonacci.py: Just select which nth term you would like to find. Doesn't work for n=0

change.py: This code will return the most precise number of quarters, nickels, dimes, ad pennies. 

